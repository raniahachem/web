{% extends 'backbase.html.twig' %}
    
      
         
{% block header %}
       
        <!-- Sidebar Start -->
        <div class="sidebar pe-4 pb-3">
            <nav class="navbar bg-secondary navbar-dark">
                <a href="dashboard" class="navbar-brand mx-4 mb-3">
              <img class="logo" src="{{ asset('img/logo.png') }}" alt="Logo" style="width: 150px;margin-top: -50px;    
margin-bottom   : -67px; ">
                </a>
                <div class="d-flex align-items-center ms-4 mb-4">
                    <div class="position-relative">
                        <img class="rounded-circle" src="img/profile.png" alt="" style="width: 40px; height: 40px;">
                        <div class="bg-success rounded-circle border border-2 border-white position-absolute end-0 bottom-0 p-1"></div>
                    </div>
                    <div class="ms-3">
                        <h6 class="mb-0">Balkis Hmidi</h6>
                        <span>Admin</span>
                    </div>
                </div>
                <div class="navbar-nav w-100">
                    <a href="dashboard" class="nav-item nav-link"><i class="fa fa-tachometer-alt me-2"></i>Dashboard</a>
                    <div class="nav-item dropdown">
                         <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"><i class="fa fa-user-edit me-2"></i>Utilisateurs</a>
                        <div class="dropdown-menu bg-transparent border-0">
                            <a href="#" class="dropdown-item">Clients</a>
                            <a href="#" class="dropdown-item">Conducteur</a>
                        </div>
                    </div>
                    <a href="#" class="nav-item nav-link"><i class="fa fa-th me-2"></i>Offres</a>
                    <a href="#" class="nav-item nav-link"><i class="fa fa-keyboard me-2"></i>Reservations</a>
                    <a  href="{{ path('app_commentaire') }}" class="nav-item nav-link  "><i class="fa fa-table me-2"></i>Commentaire</a>
                    <a href="{{ path('app_avis') }}" class="nav-item nav-link "><i class="fa fa-star me-2"></i>Avis</a>
                       <a href="/contratf/back"  class="nav-item nav-link" ><i class="far fa-file-alt me-2"></i>Contrats</a>
                    <a href="/aff" class="nav-item nav-link " ><i class="fa fa-keyboard me-2"></i>Réclamations</a>
                <a href="/message/aff" class="nav-item nav-link "><i class="fa fa-table me-2"></i>Messages</a>
                                 <a href="/abonnementf/back" class="nav-item nav-link active "><i class="fa fa-table me-2"></i>Abonnement</a>
</div>
               
            </nav>
        </div>
        <!-- Sidebar End -->



        <!-- Sidebar End -->



{% endblock %}
{% block body %}
            <div class="container-fluid pt-4 px-4">
                <div class="row g-4">
                
                    <div class="col-12">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">abonnements</h6>
                            <a href="{{ path('app_abonnementf_new') }}"> <button class="btn btn-primary" type="button">Ajouter</button></a>
                        </div>
                    </div>
                </div>
                
                
                   <div class="col-12">
    <div class="bg-secondary rounded h-100 p-4">
        <h6 class="mb-4">Responsive Table</h6>
        <div class="table-responsive">
            <form action="{{ path('app_abonnementb_index') }}" method="get" class="mb-4">
                <div class="input-group">
                    <input type="text" class="form-control" name="search_keyword" value="{{ searchKeyword }}" placeholder="Search...">
                    <button class="btn btn-primary" type="submit">Search</button>
                </div>
            </form>
            <table class="table text-start align-middle table-bordered table-hover mb-0">
                <thead>
                    <tr>
                        <th scope="col">
                            <a href="{{ path('app_abonnementb_index', {'sort_by': 'id'}) }}">
                                ID
                                                           </a>
                        </th>
                        <th scope="col">
                            <a href="{{ path('app_abonnementb_index', {'sort_by': 'type'}) }}">
                                Type
                                
                            </a>
                        </th>
                        <th scope="col">
                            <a href="{{ path('app_abonnementb_index', {'sort_by': 'prix'}) }}">
                                Prix
                                
                            </a>
                        </th>
                        <th scope="col">
                            <a href="{{ path('app_abonnementb_index', {'sort_by': 'modepaiement'}) }}">
                                Mode de Paiement
                            </a>
                        </th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                            <tbody>
                                {% for a in abonnements %}
                          <tr>
                            <td>{{ a.id }}</td>
                            <td>{{ a.typeab }}</td>
                            <td>{{ a.prixab }}</td>
                            <td>{{ a.modepaiementab }}</td>
                            <td style='display:flex'>                    
                           <a href="{{ path('app_abonnementf_edit',{'id': a.id}) }}"> <button class="btn btn-primary" type="button">Modifier</button></a>

                           <a href="{{ path('app_abonnementf_delete',{'id': a.id}) }}">
                            <button class="btn btn-danger" type="button">Supprimer</button></a>

                                </td>

                           
                          </tr>
          
                            {% else %}
                                <tr>
                                    <td colspan="4">no records found</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            </div>
{% endblock %} 
