{% extends 'backbase.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ offre.Destination }}</td>
            </tr>
            <tr>
                <tFh>Pt_depart</th>
                <td>{{ offre.ptDepart }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ offre.prix }}</td>
            </tr>
            <tr>
                <th>Type_vehicule</th>
                <td>{{ offre.typeVehicule }}</td>
            </tr>
        </tbody>
    </table>

   <a href="{{ path('app_offre_index_admin') }}">back to list</a>

     {{ include('offre/_delete_form.html.twig') }}  
{% endblock %}
